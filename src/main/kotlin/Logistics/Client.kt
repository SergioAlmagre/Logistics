package Logistics

class Client {

    var idClient:Int
    var nif:Int
    var name:String

    constructor(idClient: Int, nif: Int, name: String) {
        this.idClient = idClient
        this.nif = nif
        this.name = name
    }
}